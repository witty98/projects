<div class="row">	
	<h1>业务往来单位情况</h1>

	<h3>
	<section class="user_info col-md-4 col-md-offset-3">
	<%= link_to "新建业务单位" ,new_partyb_path %>
	<br>
		往来业务单位：
		<% if @partybs.any? %>
		  <ol class="worklogs">
			<%= render @partybs %>
		  </ol>
		  <%= will_paginate @partybs %>
		<% end %>
    </section>
	</h3>	
</div>