<div class="row">	
	<h1>合同管理</h1>
	<h3>
	<section class="user_info col-md-4 col-md-offset-3">
	<%= link_to "新建合同" ,new_contract_path %>
	<br>
		合同情况：
		<% if @contracts.any? %>
		  <ol class="worklogs">
			<%= render @contracts %>
		  </ol>
		  <%= will_paginate @contracts %>
		<% end %>
    </section>
	</h3>	
</div>